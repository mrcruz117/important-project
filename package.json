{
  "name": "important-project",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "npm install && npm install --prefix frontend && python -m venv venv && .\\venv\\Scripts\\python.exe -m pip install -r App\\requirements.txt",
    "devup": "node scripts/dev.ts",
    "test:dev": "node --test scripts/dev.test.mjs",
    "typecheck": "tsc",
    "seed": "node scripts/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2"
  }
}
